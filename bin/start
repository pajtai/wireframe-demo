#!/usr/bin/env bash

SWITCHED=false
(./bin/sass) &
(./bin/jade) &
(NODE_ENV=dev ./bin/watchify) &
cd app/public && SWITCHED=true
python -m SimpleHTTPServer

function finish {
    if [ $SWITCHED ]
    then
        cd ../..;
    fi
}

trap finish EXIT